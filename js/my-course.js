/* =========================================================
 * my-course.js — 【課題3】自作コースをゲームに登録する
 *
 * ▼ 手順
 *  1. ゲーム画面で E キー(または画面下の「🛠 コース作成」ボタン)で
 *     コースエディタを開く
 *  2. コースを作って、エディタの「⬇ ファイル保存」ボタンで
 *     JSON ファイル(◯◯.json)をパソコンに保存する
 *  3. その JSON ファイルをメモ帳や VSCode で開き、中身を全部コピーする
 *  4. 下の「MY_COURSE_JSON = null;」の null を消して、コピーした中身を
 *     そのまま貼り付ける
 *     ※ JSON は { で始まる「オブジェクト」なので、そのまま JavaScript の
 *       コードとして貼れる(これが JSON と JS の関係の面白いところ!)
 *  5. TODO【課題3】のコードを書いて、ブラウザを再読み込み
 *  → 最終ステージのあとに、自分のコースが「ステージ6」として登場する!
 *    (キャンペーンは PP.COURSES に入っているコースの数だけ続くしくみ。
 *     登録するとステージ数が自動で1つ増える)
 *
 * ▼ JSON の中身の読み方(どのキーが画面の何に対応しているか)
 *   name:     コース名。クリア画面などに表示される
 *   ctrl:     レールの制御点 [x, y] の並び。最初の点が洞窟(玉の入口)、
 *             最後の点が樽(ゴール)。画面サイズは 幅1300 × 高さ700
 *   sharp:    true なら直角に曲がる道 / false ならなめらかな曲線の道
 *   corner:   sharp のときの角の丸み(ピクセル)
 *   overpass: true なら道が自分自身と交差したとき立体交差(橋)になる
 *   lanes:    レーン(道)の配列。1本道なら要素は1つ。各レーンには
 *             ctrl(制御点)のほか、tunnels(玉が隠れるトンネル区間)や
 *             raised(橋として上を通る区間)を持たせられる
 * ========================================================= */
(function () {
  "use strict";

  // ▼▼ 手順4: ここに JSON を貼り付ける(null を消して置き換える) ▼▼
  var MY_COURSE_JSON = null;
  // ▲▲ ここまで ▲▲

  // まだ貼り付けていなければ何もしない(ゲームは通常どおり5ステージで動く)
  if (!MY_COURSE_JSON) return;

  try {
    // TODO【課題3】自作コースをゲームに登録しよう(書くのは2行!)。
    // ヒント1: PP.courseAPI.fromJSON(MY_COURSE_JSON) で、貼り付けた JSON を
    //          ゲームが使える「コースオブジェクト」に変換できる(変数に入れよう)
    // ヒント2: 変換したコースの .register() を呼ぶと PP.COURSES に追加され、
    //          新しいステージになる
    // 発展:   .register() の代わりに .play() を呼ぶと、起動した瞬間に
    //          そのコースで遊べる(動作確認に便利)
    // 確認:   実装したら index.html?level=6 で自分のステージへ直行できる



  } catch (e) {
    // 貼り付けミス(カンマ忘れなど)があってもゲーム自体は起動させる。
    // うまくいかないときは F12 のコンソールでこのメッセージを探そう。
    console.warn("【課題3】コースを登録できませんでした: " + e.message);
  }
})();
